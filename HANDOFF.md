# HANDOFF — 国税徴収法自習app 引き継ぎメモ

別PC（家のPC）でClaude Codeに続きをやってもらうための引き継ぎ書。
**続けたい作業：テキストPDFを読み込んで暗記モードの問題を追加する作業。**

---

## 0. まず家のPCでやること（セットアップ）

```bash
# 任意の作業フォルダで、GitHubからクローン
git clone https://github.com/jajaja1288/kokucho.quiz.app.git
cd kokucho.quiz.app
```

- 編集対象は **`index.html` ただ1ファイル**（自己完結のHTML/CS/JSアプリ）。
- 教材PDFは **Dropbox** にある（家のPCもDropboxがあればパスはそのままでOK）：
  `C:\Users\<ユーザー名>\Dropbox\税理士試験\国税徴収法\`
  - `1章国税と他の債権の調整.pdf`
  - `2章第二次納税義務.pdf`
  - `3章滞納処分.pdf`（144ページ・**作業中**）
  - `4章緩和措置.pdf`
- 必要ツール：`node`（検証用）、`git`（GCM認証でpush可）。PDF読込はClaude Codeの **Readツールの `pages` 指定**で行う（poppler等は不要、Readが直接PDFを画像で読める）。

---

## 1. このアプリの全体像

- スマホ対応の自習アプリ。ダークテーマ。`localStorage` 保存＋**Firebaseクラウド同期**（合言葉方式で複数端末共有）。
- 公開URL（GitHub Pages）：**https://jajaja1288.github.io/kokucho.quiz.app/**
  - **`git push` すると自動で再デプロイ**（数十秒〜1分で反映）。
- GitHubリポジトリ：`https://github.com/jajaja1288/kokucho.quiz.app.git`（Public）
- Firebase：プロジェクト `kokucho-quiz-app` / Firestore コレクション `quizdata/{合言葉}`。`firebaseConfig` は `index.html` 内に直書き（公開前提の値なので問題なし）。

### 機能の柱
1. **○×クイズ**（`CATEGORIES`）… カテゴリ別。`{a:boolean, q:'問題文', e:'解説'}`
2. **暗記モード**（`TEXT_SETS`）… 規定本文の **穴埋め（語群タップ式）** と **パズル（文節並べ替え）**。←★今やっているのはここ★
3. ホームのカレンダー・連続日数・実績（隠しご褒美実績あり）
4. 学習データのJSON書き出し／読み込み（Claude Code分析用）
5. クラウド同期

---

## 2. ★作業対象：暗記モードの問題追加

### データ構造（`index.html` 内 `const TEXT_SETS = [ ... ]`）
章の配列。各章 `{ id, name, items:[...] }`。**章番号順に並べる**（ch1→ch2→ch3→ch4）。

### 1問（item）のフォーマット
```js
{id:'t3_31', src:'問題3-3〔1〕 ○○',         // 一意なID（章プレフィックス t3_ 等）＋出典表示
 tpl:'…本文…【0】…本文…【1】…。',            // 【n】が穴埋めの空所マーカー
 blanks:[{a:'正解', d:['ダミー1','ダミー2']}, // 【0】【1】…の順。a=正解, d=語群のダミー
         {a:'正解2', d:['ダミーA','ダミーB']}],
 chunks:['文節1、','文節2、','…。']},          // パズル用。連結すると正しい全文になる文節列
```

- **ID命名**：`t<章>_<通し>`。3-1=`t3_11`〜、3-2=`t3_21`〜。3-3は `t3_31`〜 のように。**既存IDと重複させない**（IDが履歴・メモのキー）。
- `blanks` の数と `tpl` の `【n】` の数を一致させる。
- `chunks` を全部つなげると、穴埋めの正解で埋めた本文と（句読点含め）一致するように。
- **ダミー(d)** は紛らわしい近接語を選ぶと学習効果が高い（例：「50日以内」⇔「10日以内」、「法定納期限」⇔「納期限」）。

### 規定本文の取り方（重要な原則）
- **PDFから逐語で写経する。** 訓練知識（記憶）から書かない＝条文の正確性のため。
- テキストで **赤字になっているキーワード** を空所(`blanks`)／文節(`chunks`)の対象にする（赤字＝試験で問われる重要語）。
- 「問題X-Y〔n〕 見出し」を `src` に入れて出典を明示。

### 追加手順
1. ReadツールでPDFの該当ページを読む（例：`pages:"14-25"`）。1回最大20ページ。
2. その論点（問題3-3 など）の規定本文をitem化。
3. `index.html` の `TEXT_SETS` 内、**該当章 `items:[]` の末尾**（=次の章の `{ id:'chX' ...` の直前）に挿入。
4. 検証（下記）→ `git push`。

---

## 3. 進捗状況（2026-06時点）

### 暗記モード `TEXT_SETS`
| 章 | id | 問題数 | 状態 |
|---|---|---|---|
| 第1章 国税と他の債権との調整 | ch1 | 45 | ✅完了 |
| 第2章 第二次納税義務 | ch2 | 21 | ✅完了 |
| **第3章 滞納処分** | **ch3** | **22** | **🔄作業中** |
| 第4章 緩和措置 | ch4 | 29 | ✅完了 |

### 第3章の内訳（済み）
- ✅ 問題3-1 差押えの通則（15問, `t3_11`〜`t3_1d`）
- ✅ 問題3-2 差押換え（7問, `t3_21`〜`t3_28`）

### 第3章の残り（=次にやること）
- 🔜 **問題3-3 以降**（動産・有価証券／債権／不動産等の差押え、参加差押え、交付要求、換価＝公売、配当、財産調査＝質問検査・捜索 ほか。3章は全体で約18問題構成）。
- 進め方の方針：**論点（問題）ごとに区切って順次追加**（ユーザーが都度チェックしやすいように）。1論点ずつ「読む→item化→push」。
- PDFは `3章滞納処分.pdf`。問題3-1は本文p.80〜、3-2はp.96〜。3-3以降は続きのページ。「問題3-X」の見出しを目印に順番に読む。

### ○×クイズ `CATEGORIES`（参考・完了済み）
- `second` 第二次納税義務(12) / `taino` 滞納処分(35) / `kanwa` 緩和措置(9)。

---

## 4. 検証＆デプロイ

### TEXT_SETS が壊れていないかの検証（node）
```bash
node -e "const fs=require('fs');let h=fs.readFileSync('index.html','utf8');let m=h.match(/const TEXT_SETS = (\[[\s\S]*?\n\]);/);let TEXT_SETS=eval(m[1]);TEXT_SETS.forEach(s=>console.log(' -',s.name,s.items.length+'問'));"
```
→ 章名と問題数が表示されればOK（パースエラーが出なければ構造は健全）。

### デプロイ
```bash
git add index.html
git commit -m "第3章 問題3-X 暗記問題を追加"
git push
```
- pushすると GitHub Pages が自動再デプロイ。`https://jajaja1288.github.io/kokucho.quiz.app/` をリロードで反映。
- 認証：初回pushでブラウザにGitHubログイン（Git Credential Manager）が出るので承認。

---

## 5. 既存データ・同期を壊さないための注意

- **既存itemのIDを変えない／消さない**（IDが解答履歴・メモのキー。変えると過去記録が孤立する）。**追加は末尾に。**
- データ構造（`blankDB()` の形）を変える場合は、`mergeDB()`（クラウド同期の統合ロジック）と整合させること。基本は**触らない**。
- `index.html` には Firebase設定が直書きされている（公開値なのでOK）。**消さない。**

---

## 6. メモ：環境クセ

- OSはWindows。Claude CodeのBashはgit-bash寄り。日付・mkdir等が不安定ならPowerShellツールで代替。
- PDF読込はReadツールの `pages` 指定が確実（poppler不要）。
- `node` でのHTML検証は `eval` で `TEXT_SETS` だけ取り出す方式（上記4参照）。`const` だと eval スコープで未定義になるので `let X=eval('[...]')` の形にする。
